services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: pao
      POSTGRES_PASSWORD: queijo
      POSTGRES_DB: pao_de_queijo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
